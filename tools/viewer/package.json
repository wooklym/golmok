{
  "name": "golmok-viewer",
  "version": "0.1.0",
  "private": true,
  "description": "Golmok review viewer (CesiumJS). `npm install` vendors Cesium for offline use; `npm test` runs the headless smoke test against a basemap folder.",
  "scripts": {
    "test": "node test/smoke.mjs"
  },
  "dependencies": {
    "cesium": "1.145.0"
  },
  "devDependencies": {
    "playwright": "1.56.1"
  }
}
