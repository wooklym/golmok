{
  "name": "golmok-viewer",
  "version": "0.1.0",
  "private": true,
  "description": "Golmok review viewer (CesiumJS). `npm install` vendors Cesium for offline use; `npm test` runs the zonemath unit tests and the headless smoke test (basemap + zone collision/blockers meshes).",
  "scripts": {
    "test": "node test/unit.mjs && node test/smoke.mjs",
    "test:unit": "node test/unit.mjs"
  },
  "dependencies": {
    "cesium": "1.145.0"
  },
  "devDependencies": {
    "playwright": "1.56.1"
  }
}
